import streamlit as st

st.set_page_config(page_title="Smoke Test", page_icon="🧪")

st.title("✅ Deployment Successful!")
st.success("The environment is configured correctly. The issue is in the main application code.")
st.write("This is a temporary 'Hello World' app to verify the server is running.")
